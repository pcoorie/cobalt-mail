import 'package:flutter/material.dart';

/// The app's core brand blue — the same color as the app icon. Used
/// directly (rather than a scheme-derived tone) anywhere the design calls
/// for the exact, full-saturation brand color rather than a Material 3
/// tonal variant, so it can't drift between light/dark theme or between
/// call sites.
const brandSeed = Color(0xFF0A5BD6);
